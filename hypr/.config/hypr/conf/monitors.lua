-- Empty output means "apply this to all monitors".
-- Use a real output name from `hyprctl monitors` for per-monitor layouts.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = 1,
})
